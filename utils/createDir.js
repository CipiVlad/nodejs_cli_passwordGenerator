const fs = require('fs')

const createDir = (dirPath) => {
    // fs.mkdirSync(process.cwd() + dirPath, { recursive: true }, (error) => {
    //     if (error) {
    //         console.error('An error occured: ', error);
    //     } else {
    //         console.log('Your directory has benn created!');
    //     }
    // })
}
module.exports = createDir